# Description: script which is able to use crux portdb to find ports from the command prompt.
# URL: N/A 
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: bash, curl, sed, coreutils, grep, awk

name=portsearch
version=svn
release=1
source=()

build() {
 	svn co svn://mikeux.dyndns.org/sepen/projects/$name $SRC
	install -D -m 0755 $SRC/$name $PKG/usr/bin/$name
}
