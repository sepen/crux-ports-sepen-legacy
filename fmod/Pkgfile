# Description: A music & sound effects system.
# URL: http://www.fmod.org/
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on:   

name=fmod
version=4.10.01
release=1

source=(http://www.fmod.org/index.php/release/version/fmodapi41001linux.tar.gz)

build() {
  install -d ${PKG}/usr/{lib,include/fmodex}
  cp -f fmodapi41001linux/api/inc/*.h* ${PKG}/usr/include/fmodex
  cp -f fmodapi41001linux/api/lib/libfmodex.so.${version} ${PKG}/usr/lib
  cp -f fmodapi41001linux/api/lib/libfmodexp.so.${version} ${PKG}/usr/lib
  ln -sf libfmodex.so.${version} ${PKG}/usr/lib/libfmodex.so
  ln -sf libfmodexp.so.${version} ${PKG}/usr/lib/libfmodexp.so
}
