# Description: 	A music & sound effects system.
# URL: 		http://www.fmod.org/
# Maintainer:   Jose V Beneyto, joberui at ei dot upv dot es
# Packager:     Jose V Beneyto, joberui at ei dot upv dot es
# Depends on:   

name=fmod
version=3.75
release=1

source=(http://www.fmod.org/files/fmodapi375linux.tar.gz)

build() {
    install -d $PKG/usr/{lib,include/fmod}
    mv $SRC/fmodapi375linux/api/inc/* $PKG/usr/include/fmod
    mv $SRC/fmodapi375linux/api/libfmod-3.75.so $PKG/usr/lib
    ln -sf /usr/lib/libfmod-3.75.so $PKG/usr/lib/libfmod.so
}
