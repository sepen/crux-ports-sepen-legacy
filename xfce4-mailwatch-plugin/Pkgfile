# Description: Multi-threaded, multi-mailbox, multi-protocol plugin for checking mails periodically.
# URL: http://www.xfce.org
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: xfce4-panel openssl
# Group: xfce4

name=xfce4-mailwatch-plugin
version=1.0.1
release=1
source=(http://spuriousinterrupt.org/files/mailwatch/$name-$version.tar.bz2)

build () {
  cd $name-$version
  ./configure --prefix=/usr \
              --libexecdir=/usr/lib \
              --enable-ssl \
              --disable-static \
              --disable-debug
  make
  make DESTDIR=$PKG install
  rm -rf $PKG/usr/share/locale
}
