# Description: modern terminal emulator for the Unix/Linux desktop - primarly for the Xfce desktop environment.
# URL: http://terminal.os-cillation.com/
# Maintainer: Jose V Beneyto, joberui at ei dot upv dot es
# Packager: Jose V Beneyto, joberui at ei dot upv dot es
# Depends on: dbus,vte,xfce4-dev-tools,gtk,exo

name=xfce-terminal
version=0.2.5.8rc2
release=1
source=(http://download.berlios.de/xfce-goodies/Terminal-${version}.tar.bz2)

build() {
  cd Terminal-${version}
  export PKG_CONFIG_PATH=/usr/lib/pkgconfig
  ./configure --prefix=/usr --libexecdir=/usr/bin \
              --disable-startup-notification \
              --enable-debug=no
  make
  make DESTDIR=${PKG} install
  rm -rf ${PKG}/usr/share/{doc,locale}
  mv ${PKG}/usr/bin/{terminal,${name}}
}
