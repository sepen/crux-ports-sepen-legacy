# Description: Tools and script for crux tasks.
# URL: http://mikeux.dyndns.org/crux/
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: prt-utils,portdb-search

name=crux-scripts-sepen
version=2.4-svn-1112
release=1
source=()

build() {
  svn co svn://mikeux.dyndns.org/crux/branches/2.4/tools ${name}
  install -d ${PKG}/usr/bin
  rm -f ${SRC}/${name}/portdb-search
  install -m 0755 ${SRC}/${name}/* ${PKG}/usr/bin
}
