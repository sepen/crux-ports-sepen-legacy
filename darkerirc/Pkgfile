# Description:  DarkerIRC is a small chat application for websites, allowing you to communicate with your visitors.
# URL:          http://pitr.net/darkerirc/
# Maintainer:   Jose V Beneyto, joberui at ei dot upv dot es
# Packager:     Jose V Beneyto, joberui at ei dot upv dot es
# Depends on:   

name=darkerirc
version=1.3
release=1
source=(http://pitr.net/${name}/DarkerIRC_${version}-binary.tar.gz)

build() {
	install -d $PKG/var/www/htdocs
	mv DarkerIRC-${version}-binary/${name} $PKG/var/www/htdocs
	chown -R nobody:nobody $PKG/var/www/htdocs/${name}
}
