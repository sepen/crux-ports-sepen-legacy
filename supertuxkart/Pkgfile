# Description: 3D kart racing game, with many tracks, characters and items for you to try
# URL: http://supertuxkart.sourceforge.net/
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: plib

name=supertuxkart
version=0.6rc1
release=1
source=(http://dl.sourceforge.net/sourceforge/$name/$name-$version-src.tar.bz2)

build() {
  cd $name-$version
  ./configure --prefix=/usr 
  make
  make DESTDIR=$PKG install
  mv $PKG/usr/{games,bin}
  mv $PKG/usr/{share/games/$name,share/$name}
  rm -rf $PKG/usr/share/{doc,games}
  sed -e 's|usr/games|usr/bin|g' \
      -e 's|usr/share/games|usr/share|g' \
      -i $PKG/usr/share/applications/$name.desktop
  sed -r '/\[.+\]=/d' -i $PKG/usr/share/applications/$name.desktop
}
