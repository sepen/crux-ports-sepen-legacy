# Description: Ristretto is a fast and lightweight picture-viewer for the Xfce desktop environment.
# URL: http://goodies.xfce.org/projects/applications/ristretto
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: subversion,xfce4-dev-tools,thunar
# Group: xfce4

name=ristretto
version=2931-svn
release=1
source=()

build() {
  svn co -r ${version%%-*} http://svn.xfce.org/svn/goodies/${name}/trunk ${name}
  cd ${name}
  sh autogen.sh
  autoconf
  ./configure --prefix=/usr \
              --disable-debug
  make
  make DESTDIR=${PKG} install
  rm -rf ${PKG}/usr/share/locale
}
