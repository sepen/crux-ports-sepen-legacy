# Description: Xfburn is a simple CD burning tool
# URL: http://www.xfce.org/projects/xfburn/
# Maintainer: Jose V Beneyto, joberui at ei dot upv dot es
# Packager: Jose V Beneyto, joberui at ei dot upv dot es
# Depends on: subversion,intltool,xfce4-dev-tools,cdrtools,cdrdao,exo
# Group:_xfce4

name=xfburn
version=0.2.0
release=2
source=()

build() {
  svn co http://svn.xfce.org/svn/xfce/${name}/tags/${name}-${version}beta ${name}-${version}
  cd ${name}-${version}
  ./autogen.sh
  ./configure --prefix=/usr
  make
  make DESTDIR=${PKG} install
  rm -rf ${PKG}/usr/share/locale
}
