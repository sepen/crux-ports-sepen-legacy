# Description: Xfburn is a simple CD burning tool
# URL: http://www.xfce.org/projects/xfburn/
# Maintainer: Jose V Beneyto, joberui at ei dot upv dot es
# Packager: Jose V Beneyto, joberui at ei dot upv dot es
# Depends on: subversion,intltool,libxfcegui4,xfce4-dev-tools,cdrtools,cdrdao

name=xfburn
version=0.2.0
release=2
source=()

build() {
  svn co http://svn.xfce.org/svn/xfce/xfburn/tags/xfburn-0.2.0beta ${name}-${version}
  cd ${name}-${version}
  ./autogen.sh
  ./configure --prefix=/usr
  make
  make DESTDIR=${PKG} install
  rm -rf ${PKG}/usr/share/locale
}
