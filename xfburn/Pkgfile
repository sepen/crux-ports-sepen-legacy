# Description: Xfburn is a simple CD burning tool
# URL: http://www.xfce.org/projects/xfburn/
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: subversion,xfce4-dev-tools,libisofs,exo,thunar
# Group: xfce4

name=xfburn
version=0.3.0svn-26453
release=1
source=()

build() {
  svn co -r26453 http://svn.xfce.org/svn/xfce/${name}/trunk ${name}
  cd ${name}
  sed -e 's|libburn-1|libburn-5|g' -i configure.in.in
  ./autogen.sh
  ./configure --prefix=/usr \
              --enable-thunar-vfs \
              --disable-debug
  make
  make DESTDIR=${PKG} install
  rm -rf ${PKG}/usr/share/locale
}
