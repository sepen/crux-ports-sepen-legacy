# Description: Xfburn is a simple CD burning tool
# URL: http://www.xfce.org/projects/xfburn/
# Maintainer: Jose V Beneyto, sepen at users dot sourceforge dot net
# Packager: Jose V Beneyto, sepen at users dot sourceforge dot net
# Depends on: subversion xfce4-dev-tools libisofs thunar startup-notification
# Group: xfce4

name=xfburn
version=0.3.0svn-26662
release=1
source=(xfburn-libisofs-0.6.2.patch)

build() {
  svn co -r26662 http://svn.xfce.org/svn/xfce/$name/trunk $name
  cd $name
  patch -p0 < $SRC/xfburn-libisofs-0.6.2.patch
  ./autogen.sh
  ./configure --prefix=/usr \
              --enable-thunar-vfs \
              --disable-debug
  make
  make DESTDIR=$PKG install
  rm -rf $PKG/usr/share/locale
}
