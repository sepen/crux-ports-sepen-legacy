# Description: Smart URI fetching/caching
# URL: http://search.cpan.org/~btrott/URI-Fetch-0.08/
# Maintainer: Jose V Beneyto, joberui at ei dot upv dot es
# Packager: Jose V Beneyto, joberui at ei dot upv dot es
# Depends on: p5-compress-zlib, p5-cache

name=p5-uri-fetch
version=0.08
release=1
source=(http://search.cpan.org/CPAN/authors/id/B/BT/BTROTT/URI-Fetch-0.08.tar.gz)

build() {
	cd URI-Fetch-$version
	perl Makefile.PL
	make OPTIMIZE="${CFLAGS}"
	make DESTDIR=$PKG install
	find $PKG -type f -name perllocal.pod -exec rm '{}' \;
	find $PKG -type f -name .packlist -exec rm '{}' \;	
}
